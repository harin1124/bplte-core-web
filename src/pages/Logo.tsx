import styles from './Logo.module.css';

const Logo = () => {
  return (
    <div className={styles.logo}>
      <span>🔥</span>
      <h1>Boilerplate Core</h1>
    </div>
  );
};

export default Logo;
